name: Maintain Ardoq Tech Stack
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on:
  push:
    branches:
      - master
      - main
      - ardoq-integration
jobs:
  Maintain-Ardoq-Tech-Stack:
    runs-on: ubuntu-latest
    steps:
      - run: echo "🔎 The name of your branch is ${{ github.ref }} and your repository is ${{ github.repository }}."
      - uses: actions/checkout@v3
      - name: Set up JDK 11
        uses: actions/setup-java@v3
        with:
          java-version: '11'
          distribution: 'adopt'
      - name: Dump dependencies
        run: ./gradlew -q dependencies > $PWD/deps.log
      - run: cat $PWD/deps.log
      - name: Send data to Ardoq adapter
        env:
          ARDOQ_ADAPTER_URL: ${{ secrets.ARDOQ_ADAPTER_URL }}
        run: |
          curl -X POST $ARDOQ_ADAPTER_URL/api/gradle/send-letter-service \
          -H 'Content-Type: text/plain' \
          --data-binary "@$PWD/deps.log"
      - run: echo "🍏 This job's status is ${{ job.status }}."
